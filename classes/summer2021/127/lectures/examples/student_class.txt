
class Student():

    #constructor
    def __init__(self,name,major,student_id,gpa="N/A",year="Freshman"):
        self.name = name
        self.major = major
        self.gpa = gpa
        self.student_id = student_id
        self.year = year
        self.minor = "N/A"
        self.champ_change = 0

    #reader methods
    def getName(self):
        return self.name
    def getMajor(self):
        return self.major
    def getGPA(self):
        return self.gpa
    def getStudentID(self):
        return self.student_id
    def getYear(self):
        return self.year
    def getMinor(self):
        return self.minor

    #writer methods
    def setMinor(self,new_minor):
        self.minor = new_minor
    def setMajor(self,new_major):
        self.major = new_major

    #special str method (overrides what gets printed out)
    def __str__(self):
        answer = self.name + " is a " + self.year + " studying " + self.major
        return answer


    def determine_years_left(self):

        if(self.year == "Freshman"):
            print("You have ~3 years left")
        elif(self.year == "Sophomore"):
            print("You have ~2 years left")
        elif(self.year == "Junior"):
            print("You have ~1 year left")
        elif(self.year == "Senior"):
             print("You have less than one year left")


    

def main():

    #want to create a new student
    # James, Computer Science, 4.0, 123456789, Junior
    new_student = Student("Reese","Sociology","1A2B3C")
    transfer_student = Student("Sarah","Biology","1634634","3.1","Sophomore")


    print(new_student)
    new_student.setMajor("Computer Science")
    print(new_student)

    new_student.determine_years_left()




    
    

main()
